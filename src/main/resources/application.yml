server:
  port: 8080

spring:
  application:
    name: stock-tracker
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/stocktracker}

brapi:
  base-url: https://brapi.dev/api
  token: ${BRAPI_TOKEN:}
